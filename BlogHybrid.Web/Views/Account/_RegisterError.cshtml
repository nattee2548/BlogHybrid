@* ========================================= *@
@* Views/Account/_RegisterError.cshtml *@
@* ========================================= *@

<div id="register-form-container" class="text-center">
    <div class="error-animation">
        <div class="error-icon">
            <i class="fas fa-exclamation-triangle"></i>
        </div>
        <h3 class="text-danger mt-3">เกิดข้อผิดพลาด</h3>
        <p class="text-muted">ไม่สามารถสร้างบัญชีได้ในขณะนี้</p>

        <div class="mt-4">
            <button type="button"
                    class="btn btn-outline-primary"
                    onclick="location.reload()">
                <i class="fas fa-redo me-2"></i>ลองใหม่อีกครั้ง
            </button>
        </div>
    </div>
</div>

<script>
    // Trigger error event
    document.dispatchEvent(new CustomEvent('registration-error'));
</script>